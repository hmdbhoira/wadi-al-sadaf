---
// Machinery domain footer
const baseUrl = import.meta.env.BASE_URL || '/';
---

<footer class="bg-[var(--brand-black)] text-white mt-auto border-t-4 border-[var(--brand-gold)]">
  <div class="container mx-auto px-4 py-12">
    <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
      <!-- Company Info -->
      <div>
        <img src={`${baseUrl}images/logo.jpeg`} alt="Wadi Al Sadaf" class="h-16 w-16 object-contain mb-4 rounded-full shadow-md" />
        <h3 class="text-xl font-bold text-white mb-2">Wadi Al Sadaf</h3>
        <p class="text-sm text-gray-300 mb-4 leading-relaxed">
          Your trusted partner in industrial automation, electrical, and instrumentation solutions.
        </p>
      </div>

      <!-- Quick Links -->
      <div>
        <h4 class="text-lg font-bold text-[var(--brand-gold)] mb-4">Quick Links</h4>
        <ul class="space-y-2 text-sm">
          <li><a href={`${baseUrl}machinery/`} class="text-gray-300 hover:text-[var(--brand-gold)] transition-colors">Home</a></li>
          <li><a href={`${baseUrl}machinery/products/`} class="text-gray-300 hover:text-[var(--brand-gold)] transition-colors">Products</a></li>
          <li><a href={`${baseUrl}machinery/about/`} class="text-gray-300 hover:text-[var(--brand-gold)] transition-colors">About Us</a></li>
          <li><a href={`${baseUrl}machinery/contact/`} class="text-gray-300 hover:text-[var(--brand-gold)] transition-colors">Contact</a></li>
        </ul>
      </div>

      <!-- Product Categories -->
      <div>
        <h4 class="text-lg font-bold text-[var(--brand-gold)] mb-4">Our Products</h4>
        <ul class="space-y-2 text-sm">
          <li><a href={`${baseUrl}machinery/electrical/`} class="text-gray-300 hover:text-[var(--brand-gold)] transition-colors">Electrical</a></li>
          <li><a href={`${baseUrl}machinery/automation/`} class="text-gray-300 hover:text-[var(--brand-gold)] transition-colors">Automation</a></li>
          <li><a href={`${baseUrl}machinery/instrumentation/`} class="text-gray-300 hover:text-[var(--brand-gold)] transition-colors">Instrumentation</a></li>
        </ul>
      </div>

      <!-- Contact Info -->
      <div>
        <h4 class="text-lg font-bold text-[var(--brand-gold)] mb-4">Contact Us</h4>
        <ul class="space-y-3 text-sm text-gray-300">
          <li class="flex items-start gap-2">
            <span class="mt-1">📍</span>
            <span>
              Dubai Industrial City, 17R-SH-493<br />
              Saih Shuaib 3, Dubai, UAE
            </span>
          </li>
          <li class="flex items-start gap-2">
            <span class="mt-1">📱</span>
            <a href="tel:+971554274256" class="hover:text-[var(--brand-gold)] transition-colors">
              +971 55 427 4256
            </a>
          </li>
          <li class="flex items-start gap-2">
            <span class="mt-1">✉</span>
            <a href="mailto:wadialsadaf.sales@gmail.com" class="hover:text-[var(--brand-gold)] transition-colors break-all">
              wadialsadaf.sales@gmail.com
            </a>
          </li>
        </ul>
      </div>
    </div>

    <!-- Bottom bar -->
    <div class="border-t border-[var(--brand-gold)]/30 mt-8 pt-8 text-center text-sm text-gray-400">
      <p>&copy; {new Date().getFullYear()} Wadi Al Sadaf General Trading LLC - Machinery Division. All rights reserved.</p>
    </div>
  </div>
</footer>
